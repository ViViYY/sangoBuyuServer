const defines = {};
defines.get = 'get';
defines.set = 'set';
defines.both = 'both';

defines.roomPlayerNumberMax = 4;
defines.roomNumberMax = 1;

defines.roomType = {
    simple:1,
    hard:2,
};

module.exports = defines;